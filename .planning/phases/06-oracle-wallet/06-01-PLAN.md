---
phase: 06-oracle-wallet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dcx-oracle/lib/oracle_wallet.sh
  - dcx-oracle/tests/test_oracle_wallet.sh
autonomous: true

must_haves:
  truths:
    - "Wallet location can be configured"
    - "Wallet validity is checked (cwallet.sso)"
    - "TNS_ADMIN is set correctly for wallet usage"
  artifacts:
    - path: "dcx-oracle/lib/oracle_wallet.sh"
      provides: "wallet_set_location, wallet_check_valid, wallet_configure_env"
---

<objective>
Implement Oracle Wallet management library to support secure credential storage without 'mkstore'.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create dcx-oracle/lib/oracle_wallet.sh</name>
  <files>dcx-oracle/lib/oracle_wallet.sh</files>
  <action>
    Create library with functions:
    - `wallet_set_location(path)`: Set global wallet path.
    - `wallet_check_valid(path)`: Check for cwallet.sso.
    - `wallet_configure_env(path)`: Export TNS_ADMIN and WALLET_LOCATION.
  </action>
</task>

<task type="auto">
  <name>Task 2: Create tests</name>
  <files>dcx-oracle/tests/test_oracle_wallet.sh</files>
  <action>
    Create tests to verify environment variables and file checks.
  </action>
</task>
</tasks>
