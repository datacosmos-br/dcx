# DCX/bin - Binary Directory
#
# This directory contains bundled tools and CLI scripts.
# Some files are committed to git, others are generated during build/release.
#
# COMMITTED TO GIT (always present):
# - dcx               : Main CLI wrapper script (bash)
# - .gitkeep          : This documentation file
#
# GENERATED (not in git, created by build/release process):
# - gum-*             : Go terminal UI toolkit
# - yq-*              : YAML/JSON processor
# - rg-*              : Fast grep (ripgrep)
# - fd-*              : Fast find
# - sd-*              : Fast sed
# - frawk-*           : Fast awk
# - coreutils-*       : Rust coreutils
# - bash-*            : Static bash binary
#
# Naming convention: {tool}-{os}-{arch}
# Examples:
#   gum-linux-amd64, yq-darwin-arm64, rg-windows-amd64
#
# Supported platforms:
# - linux-amd64, linux-arm64, linux-386
# - darwin-amd64, darwin-arm64
# - windows-amd64, windows-arm64, windows-386
#
# Build commands:
#   make binaries         # Build for all platforms (requires Go, Rust, cross-compilation)
#   make binaries-local   # Build for current platform only
#   make release          # Create release tarball with all binaries
#   make publish          # Upload release to GitHub Releases
#
# Installation:
#   Binaries are downloaded automatically by install.sh from GitHub Releases
#   For local development, build with: make binaries-local
